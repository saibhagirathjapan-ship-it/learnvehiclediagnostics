# Dependencies (installed with --no-save; never committed)
node_modules/

# Verification screenshots — the site is self-contained (inline SVG + base64 fonts),
# so these PNGs are build/verification artifacts, not part of the website.
*.png

# Derivation working artifacts, incl. txt/ local working artifacts
_derive/

# Design-exploration scratch (not part of the shipped website)
_designlab/
